# 開発者アカウントの設定

## 更新
1. index.htmlとscript.jsを今のゲームフォルダーへ上書きし、GitHubにもアップロード。
2. Firebaseコンソール → Firestore Database → ルールに、同梱firestore.rulesの全文を貼り付けて公開。
3. 設定済みfirebase-config.jsは上書きしない。

## testを登録
1. ゲームの新規登録画面で名前test、会話で指定したパスワードを入力して新規登録。
   すでにtestがある場合は、その本人のアカウントにログインする。
   Firebase Authentication画面から適当なメールアドレスで新規作成しないでください。
   ゲームが名前から内部メールアドレスを生成するため、ゲーム画面からの登録が必要です。
2. Firebaseコンソール → Firestore Database → データ → playersを開く。
3. usernameがtestのドキュメントを探す。
4. 「フィールドを追加」で次を設定して保存。
   フィールド名: isDeveloper
   型: boolean
   値: true
5. ゲームからログアウトしてtestでログインし直す。
6. 名前の横に「開発者・ランキング対象外」と出れば完了。

このZIPだけではFirebase上のアカウントは作成されません。
パスワードはHTML/JavaScriptやこの文書に収録していません。
名前だけでは開発者権限になりません。Firebaseコンソールの設定で指定します。
開発者権限の解除は同じフィールドをfalseにして、ゲームにログインし直してください。

## テスト用強化設定
元のゲームは上限なしの育成なので「全項目の最大値」は存在しません。
開発者用には以下の高強化プリセットを用意しています。
- 所持金: 最低1,000,000G
- 攻撃力Lv100 / 連射Lv11（通常強化での最短間隔100ms）
- 同時発射Lv10 / 弾サイズLv6 / 移動速度Lv93 / 最大体力Lv100
- 全4スキル所有、各Lv20
- エンドレス即時解放、全4キャラクター利用可能
- ログイン・戦闘開始・再挑戦時にプリセットを適用
- 無敵ではありません。スキルのクールタイムも通常どおりです。

開発者用のセーブは一般用と分離。権限解除後に強化を一般データへ持ち込みません。
開発者アカウントの挑戦回数・WAVE記録は送信せず、ランキング表示からも除外します。
既存記録がある場合も表示から除外しますが、Firestore内の過去記録自体は消しません。
ランキング除外の反映には、閲覧者も新しいページを読み込む必要があります。
アカウントのデータリセットでは開発者フラグは解除されません。解除はコンソールで行ってください。

## 確認
構文・既存回帰・一般アカウントへの影響・開発者強化・セーブ分離・ランキング除外と送信抑止を模擬テスト済み。
実Firebaseルールエミュレーターと実ブラウザでの接続テストは未実施。
